---
title: "Providers"
description: "Supported LLM providers for Allternit"
---

# Providers

Allternit supports multiple LLM providers.

## Claude (Anthropic)

```yaml
providers:
  claude:
    api_key: ${CLAUDE_API_KEY}
    model: claude-sonnet-4-20250514
```

**Available Models:**
- `claude-opus-4-20250514` — Most capable
- `claude-sonnet-4-20250514` — Balanced
- `claude-haiku-4-20250514` — Fastest

## OpenAI

```yaml
providers:
  openai:
    api_key: ${OPENAI_API_KEY}
    model: gpt-4o
```

**Available Models:**
- `gpt-4o` — Best overall
- `gpt-4o-mini` — Cost-effective

## Kimi (Moonshot)

```yaml
providers:
  kimi:
    api_key: ${KIMI_API_KEY}
    base_url: https://api.moonshot.cn/v1
    model: kimi-k2
```

## Gemini (Google)

```yaml
providers:
  gemini:
    api_key: ${GEMINI_API_KEY}
    model: gemini-2.5-pro
```

## Local Models

Use Ollama for local execution:

```yaml
providers:
  local:
    base_url: http://localhost:11434
    model: llama3.2
```
